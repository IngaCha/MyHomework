/**
 * Program that prints Hello + name you write in terminal.
 * 
 * @author Inga Chamentauskiene
 *
 */

class HelloName {
	public static void main(String[] name) {
		// Prints "Hello" + name to the terminal window.
		System.out.println("Hello " + name[0] + "!");
	}
}
